export type RegisterData = {
  username: string;
  email: string;
  password: string;
  role: string;
};

export type RegisterResponse = {
  message: string;
  // TODO: Change this to the correct type
};
